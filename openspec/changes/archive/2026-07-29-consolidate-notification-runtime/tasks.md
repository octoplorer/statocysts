## 1. 通知领域模型

- [x] 1.1 新增公开的 `Notification`、`Notifier` 与失败明细类型，并实现不修改内容的共享通知校验
- [x] 1.2 新增公开的 `NotificationDeliveryError`，包含失败目标、原始原因、成功数和失败数
- [x] 1.3 为通知字段类型、空白标题、内容保留和错误数据编写测试

## 2. 深通知运行时

- [x] 2.1 实现内部通知运行时构建函数，并在构建时验证通知提供方形状和协议唯一性
- [x] 2.2 实现 `createNotifier()` 的非空列表、字符串地址、标准规范化、不支持协议和重复目标校验及目标绑定缓存
- [x] 2.3 实现通知器无上限并行投递、等待全部完成、成功返回 `undefined` 和无重试行为
- [x] 2.4 实现多目标与单目标运行时失败到 `NotificationDeliveryError` 的统一聚合，同时保留直接提供方原始错误
- [x] 2.5 使用内部记录通知提供方编写运行时接口测试，覆盖创建校验、延后专属校验、并行启动、全部等待和聚合结果

## 3. 提供方目录与入口适配器

- [x] 3.1 建立浏览器端静态提供方目录，纳入所有浏览器可用提供方并补充 `serverChan` 与 `jsons` 命名导出
- [x] 3.2 建立服务端静态提供方目录，复用浏览器端目录并额外纳入 `email`
- [x] 3.3 添加目录测试，验证两个环境的预期协议集合、命名导出与运行时注册集合一致
- [x] 3.4 将 `index.ts` 与 `browser.ts` 改为使用各自目录构建运行时，并公开 `send()`、`createNotifier()`、领域类型、聚合错误和具体提供方
- [x] 3.5 让 `defineProvider()` 复用共享通知校验，验证所有具体通知提供方直接调用具有一致校验行为

## 4. 移除旧接口

- [x] 4.1 删除 `createSender()`、`senderRegistry`、`resolveProvider()` 与旧浅注册表实现
- [x] 4.2 停止从包入口导出 `defineProvider`、`defineTransport`、`buildSenderRegistry`、内置传输对象及相关内部类型
- [x] 4.3 删除旧注册表实现测试，并添加公开导出与生成类型声明检查以确认旧接口不可用

## 5. 调用方迁移与文档

- [x] 5.1 将命令行入口迁移到结构化通知和 `createNotifier()`，并使用 `NotificationDeliveryError` 输出逐目标失败后以非零状态结束
- [x] 5.2 将 README 和通知提供方文档中的位置参数、`createSender()` 与旧注册表示例迁移到新接口
- [x] 5.3 更新 QQ 机器人相关测试，验证服务端与浏览器端目录中的注册和顶层命名导出

## 6. 验证

- [x] 6.1 运行完整单元测试与类型检查并修复本变更引入的问题
- [x] 6.2 运行构建与发布接口检查，核对服务端和浏览器端产物的公开导出及类型声明
- [x] 6.3 运行代码检查和 OpenSpec 变更校验，确认实现满足所有新增与修改场景
