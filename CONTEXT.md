# 通知投递

Statocysts 使用统一的通知表达，将消息投递到由协议地址标识的目标，并允许调用方选择具体的通知提供方。

## 领域语言

**通知**：
一次待投递的消息，由必填标题和可选正文组成。
_避免使用_：消息、内容

**通知目标**：
由协议地址标识的投递目的地；地址携带选择通知提供方及定位接收方所需的信息。
_避免使用_：地址、端点

**通知提供方**：
理解特定协议地址并能将通知投递到对应通知目标的能力。
_避免使用_：通知服务、渠道

**通知器**：
与一个或多个通知目标绑定、可重复接收并投递通知的对象。
_避免使用_：发送器

**通知运行时**：
协调通知、通知目标与通知提供方，并统一执行解析、分发和投递策略的入口。
_避免使用_：注册表、发送器
