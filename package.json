{
  "name": "@statocysts/monorepo",
  "type": "module",
  "private": true,
  "packageManager": "pnpm@12.3.4",
  "license": "MIT",
  "scripts": {
    "up": "nlx taze",
    "build": "pnpm --filter 'statocysts' run build && pnpm --filter '@statocysts/cli' run build",
    "docs:dev": "pnpm --filter '@statocysts/docs' run dev",
    "docs:build": "pnpm --filter '@statocysts/docs' run build",
    "docs:preview": "pnpm --filter '@statocysts/docs' run preview",
    "docs:check": "pnpm --filter '@statocysts/docs' run typecheck && pnpm run docs:build",
    "test": "pnpm --filter 'statocysts' run test && pnpm --filter '@statocysts/cli' run test",
    "lint": "eslint --cache .",
    "lint:fix": "eslint --cache . --fix",
    "typecheck": "pnpm -r typecheck",
    "release": "bumpp -r --execute=\"pnpm i\"",
    "postinstall": "simple-git-hooks"
  },
  "devDependencies": {
    "@antfu/eslint-config": "catalog:dev",
    "@antfu/ni": "catalog:dev",
    "bumpp": "catalog:dev",
    "eslint": "catalog:dev",
    "eslint-plugin-format": "catalog:dev",
    "lint-staged": "catalog:dev",
    "simple-git-hooks": "catalog:dev",
    "taze": "catalog:dev",
    "typescript": "catalog:dev"
  },
  "simple-git-hooks": {
    "pre-commit": "npx lint-staged"
  },
  "lint-staged": {
    "*.{js,ts,tsx,vue,md}": [
      "eslint --cache --fix"
    ]
  }
}
